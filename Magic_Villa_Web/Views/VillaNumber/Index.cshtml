@model IEnumerable<VillaNumberDTO>
<br />
<div class="container p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">Villa Number List</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="VillaNumber" asp-action="Create" class="btn btn-outline-primary"><i class="bi bi-plus-square-fill"></i>Create new villa number</a>
        </div>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th>Villa Name</th>
                <th>Villa Number</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach(var item in Model)
            {
                <tr>
                    <td>@item.Villa.Name</td>
                    <td>@item.Number</td>
                    <td>
                        <a asp-controller="VillaNumber" asp-action="Update" asp-route-id="@item.Number" class="btn btn-secondary"><i class="bi bi-pencil-square"></i></a>
                        <form class="form-check-inline" asp-controller="VillaNumber" asp-action="Delete" asp-route-id="@item.Number" method="post">
                            <button class="btn btn-danger text-white" type="submit" onclick="return confirm('Are you sure you want to delete @item.Number')"><i class="bi bi-trash-fill"></i></button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
